#include <iostream>
using namespace std;

int main() {
    int N,M; cin >> N >> M;
    int x, suma=0;
    for (int i=0;i<N;++i) for (int j=0;j<M;++j){ cin>>x; suma+=x; }
    cout << "Suma total: " << suma << endl;
    return 0;
}
